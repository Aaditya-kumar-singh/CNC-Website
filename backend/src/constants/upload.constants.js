const MAX_PREVIEW_IMAGE_SIZE_BYTES = 2 * 1024 * 1024;
const MAX_CNC_FILE_SIZE_BYTES = 35 * 1024 * 1024;
const CLOUDINARY_CNC_CHUNK_SIZE_BYTES = 6 * 1024 * 1024;

module.exports = {
    MAX_PREVIEW_IMAGE_SIZE_BYTES,
    MAX_CNC_FILE_SIZE_BYTES,
    CLOUDINARY_CNC_CHUNK_SIZE_BYTES,
};
